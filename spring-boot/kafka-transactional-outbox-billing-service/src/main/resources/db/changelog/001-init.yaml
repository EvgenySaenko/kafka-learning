databaseChangeLog:
  - changeSet:
      id: 001-create-billing-dummy
      author: evgeny
      changes:
        - sql:
            sql: |
              CREATE TABLE IF NOT EXISTS outbox_billing.billing_dummy (
                id BIGSERIAL PRIMARY KEY,
                created_at TIMESTAMPTZ NOT NULL DEFAULT now()
              );
